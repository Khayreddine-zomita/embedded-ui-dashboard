Specialized reusable controls live here. Widget files should start with a `<widget>` tag.

Use widgets for lower-level controls or custom drawing elements, then compose them inside components or screens.
