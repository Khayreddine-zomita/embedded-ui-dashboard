# GL-Towers UI State Contract

`gl_towers_ui_contract.h` defines the data the LVGL UI expects from firmware and the commands the UI can send.

`gl_towers_mock_state.c` provides demo values so the UI can be previewed before real sensors are integrated.
